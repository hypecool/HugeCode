{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base capability for the HugeCode main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
